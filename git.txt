--------云端IDE同步GIT测试---------


--配置GIT
    -git config --global user.name "mmys-zh"
    -git config --global user.email "2577271950@qq.com"
--忽略文件配置
    .gitignore文件
--初始化仓库
    -git init
--检查状态
    -git status
--将文件加入到仓库
    -git add .
--执行提交
    -git commit -m "commit"
    -git commit -am "commit"
--查看提交记录
    -git log 【全部信息】
    -git log --pretty=oneline 【简要信息】
--撤销修改
    -git checkout .
